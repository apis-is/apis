describe('Metrics Middleware', () => {
  // TODO: mock redis here and make sure redis gets called with data when a endpoint is hit
  it('should record how often a endpoint is called')
  // TODO: mock redis here, fill it with mock data and make sure the results from calling the metrics endpoint are what
  // we expect to get back
  it('should display how often a endpoint is called')
})
